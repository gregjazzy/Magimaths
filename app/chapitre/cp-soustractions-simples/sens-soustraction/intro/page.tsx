import ChapterIntroPage from '@/components/chapter/ChapterIntroPage';
import config from '@/config/chapters/cp-soustraction-sens.json';

export default function IntroPage() {
  return <ChapterIntroPage config={config} />;
}
